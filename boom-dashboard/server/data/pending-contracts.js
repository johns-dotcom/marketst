// Seed data for pending_contracts table.
// Loaded by index.js → runMigrations() when the table is empty on first deploy.
// Each row: [artist_name, legal_name, address, cash, split, years, options, back_signs, futures, status, email]
// Market Street starts with an empty pipeline — add deals from the Contracts page.

module.exports = [];
