# Production environment — baked into the Vite build at deploy time.
# VITE_API_URL falls back to /api in production (set in api.js), so no need to repeat it here.
VITE_BK_URL=https://marketst-production.up.railway.app
